import { StyleSheet } from "react-native";

export default StyleSheet.create({
    Text : {
        fontFamily: "Roboto",
        color: "#FFF",
        fontSize: 16,
        fontWeight: 500
    }
});